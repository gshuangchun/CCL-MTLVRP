# @package _global_

# Override defaults by taking another config
defaults:
  - /experiment/rfbase-100.yaml
  - _self_

env:
  generator_params:
    variant_preset: "all"

model:
  _target_: routefinder.models.model.RouteFinderBase
  policy:
    _target_: routefinder.models.policy.RouteFinderPolicy
    normalization: "rms"
    encoder_use_prenorm: True
    encoder_use_post_layers_norm: True
    parallel_gated_kwargs:
      mlp_activation: "silu"

# Logging: we use Wandb in this case
logger:
  wandb:
    tags: ["rf", "${env.name}"]
    group: "${env.generator_params.num_loc}-${env.name}"
    name: "0424-routefinder-LO-CaDA-ReLD-dotNoise-P75-newO-${env.generator_params.num_loc}-main"
    project: "0210-routefinder"

#ckpt_path: "/common/home/users/s/scgui/Code/2025-LLMCO/0412-routefinder-LO-CaDA-contextSplit-ReLD-dist-NodeDec-attDist-gate1-crossNode/logs/0412-routefinder-LO-CaDA-contextSplit-ReLD-dist-NodeDec-attDist-newgate1-crossNode-h200-P20-newO-100-main/2025-04-17_02-51-27/checkpoints/epoch_133.ckpt"